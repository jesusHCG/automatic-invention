version: 0.1.1
env:
  - TIMEOUT: 30

check_changelog_updated_on_pr: false
pipeline_os: "Windows"

job_parameters:
    -
      name: "Branch"
      defaultValue: ""
      description: "Mi primera descripcion"
    -
      name: "Repo"
      defaultValue: ""
      description: "Mi segunda descripcion"

build:
  before_scripts:
    - "powershell.exe -ExecutionPolicy ByPass -File .\\DockerUtilities\\SecondPreBuild.ps1"
